#Here are the data path vairables, change them accordingly

export extr_host=accessdev.nci.org.au
export RHOST_NAME=raijin.nci.org.au
export RUNID=coastal01 #The id of the experiment
export UM_RDATADIR=$HOME/Programming/UM/coastal #The extract location on extr_host
export UM_ROUTDIR=$UM_RDATADIR #The extract locatoin on RHOST_NAME

#This variables are needed but don't have to be necessarily changed
export DATAW=/short/$PROJECT/$USER #The path where the output is saved
export USERID=$USER  #The user id
